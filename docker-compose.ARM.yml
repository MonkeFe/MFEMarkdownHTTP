services:
  web:
    image: mfe-markdown-http-arm:latest
    environment:
      - USER=${USER}
      - HASHED_PASSWORD=${HASHED_PASSWORD}
    volumes:
      - ./files:/app/files
    ports:
      - "3000:3000"
